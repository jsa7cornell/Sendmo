# SendMo — Wishlist

> Running list of improvements, bugs, and polish items. Prioritize from top down.
> Add new items at the bottom. Mark items `[x]` when shipped.

---

## 🚨 Launch blockers

- [x] **`admin_insert_shipment` RPC fails on every label call — shipments have not been persisted to the DB since March 18** — 🚨 **LAUNCH BLOCKER. CRITICAL.** Discovered 2026-05-12 during Phase A smoke test (UPS Ground `1Z13J52C0333598579`). Event log evidence: only ONE `label.db_persisted` row exists in `event_logs` across the entire history (2026-03-18). Every label since then has been bought successfully from EasyPost but **never written to the `shipments` table.** Two compounding root causes: (1) **Function overload collision** — production has TWO `admin_insert_shipment` overloads sitting alongside each other. Migration 014's `DROP FUNCTION IF EXISTS` targeted the 29-param signature it expected from migration 012, but migration 012 may not have applied cleanly to prod, so the DROP was a no-op and the new TABLE-returning version landed as a sibling to the surviving UUID-returning version from migration 008. PostgREST function lookup can't resolve which overload to call. (2) **Frontend address shape bug** — `addressToApi` in `src/lib/api.ts` maps `addr.street → street1`, but at runtime `addr.street` is `undefined` despite the `AddressInput.street: string` type claim. JSON.stringify drops undefined values, so `from_address.street1` and `to_address.street1` are missing from the labels function call, which causes the RPC param lookup to fail against the version that has those as required-no-default params. Fix has three parts: (a) Write migration 018 that explicitly drops both overloads and recreates the canonical TABLE-returning version, (b) trace where `addr.street` becomes undefined in the flow (likely `RecipientStepFullShipping` or the smart-address-input wiring) and make the field actually populated, (c) consider tightening `addresses.street1` constraint enforcement so future failures are visible at the function call site instead of bubbling up as PostgREST 404s. **Until fixed, every Full Label flow generates an EasyPost label without a corresponding shipments row** — meaning the admin report, dashboard, /admin reconciliation, webhook tracker.updated lookups, all of it has no shipment to join against. Phase A's `comp_grant` smoke test exposed this; Phase A is not the cause.

- [x] **EasyPost webhook HMAC verification rejecting all events** — 🚨 **LAUNCH BLOCKER.** Nine consecutive `webhook.hmac_invalid` events in `event_logs` with `reason: signature_mismatch` between 22:49 and 22:54 on 2026-05-12. EasyPost is POSTing `tracker.updated` events (and likely others) to `webhooks/index.ts`, but our HMAC validation is failing every signature check. Result: no tracking-update DB writes, no notification dispatches on delivery, every shipment's status stays at `label_created` forever. Likely root cause: `EASYPOST_WEBHOOK_HMAC_SECRET` configured in Supabase doesn't match the secret EasyPost is signing with (rotation drift, wrong project's secret, or initial setup mismatch). Fix: (a) confirm the secret in Supabase function env via `npx supabase secrets list --project-ref fkxykvzsqdjzhurntgah`, (b) reconcile against the value in the EasyPost dashboard webhook settings, (c) rotate both sides if they've drifted. Until fixed, no shipment transitions to `in_transit` / `delivered`, and the WISHLIST "Tracking status stuck" bug regresses despite the prior `tracker.updated` URL registration.

- [x] **Share Link on Label & Link step is hardcoded `sendmo.co/s/test`** — 🚨 **LAUNCH BLOCKER.** After a Full Prepaid Label flow completes (Label & Link step), the Share Link card surfaces a placeholder URL (`sendmo.co/s/test`) instead of a real shareable link tied to this shipment. The Full Label path doesn't write a `sendmo_links` row, so there's no real `short_code` to surface. Recipients can't share the label with anyone today. Confirmed live 2026-05-12 — see UPS Ground `1Z13J52C0333598579` Phase A smoke-test screenshot. Two-part fix: (1) labels function (or a new step) writes a `sendmo_links` row with a generated `short_code` after the shipment lands, owned by the verified recipient; (2) RecipientStepLinkReady reads the real `short_code` and displays `sendmo.co/s/<code>`. Until this lands, the Full Prepaid Label flow is end-to-end demo-able but not actually launchable. Related: the existing "Full Label flow doesn't create account or link" item below covers the account-creation half (which shipped 2026-05-11 via the OTP rework); the link-creation half is what remains. **Cross-link:** the persistence bug above means there's no shipment row to attach a `sendmo_links` row to anyway, so the persistence fix is a prerequisite to this one.

---

## UX / Polish

- [ ] **Signed-in users should land on their dashboard, not the public homepage** — When an authenticated user hits `/` (or returns to the site), redirect to their dashboard/home rather than rendering the marketing landing page. Public landing should only show for unauthenticated visitors.
- [ ] **Production-quality headers and graphics** — Add SendMo logo to nav, email templates, landing page. Replace placeholder text/icons with branded assets. Consistent header across all pages.
- [ ] **Favicon + logo refresh** — Replace the placeholder favicon and any inline logo SVGs with a proper SendMo branded set (favicon.ico, apple-touch-icon, 192/512 PWA icons, OG image). Tab and home-screen icons currently look unbranded.
- [ ] **Label download link should be secure** — After Full Label flow completes, the label PDF link should be a signed/expiring URL (not a public EasyPost URL). Prevents unauthorized access to shipping labels.

## Bugs

- [x] **Tracking status stuck "In Transit" after delivery; no notification emails sent** — Example: Barb Anderson shipment, USPS tracking `94346362083033...`. Investigation 2026-04-26 surfaced four separate bugs; all four fixed in commit `ebedd4e` and EasyPost webhook registered the same day:
    1. ✅ EasyPost `tracker.updated` webhook URL registered in EasyPost dashboard.
    2. ✅ `notification_contacts` now populated on label buy (`src/lib/api.ts` `buyLabel` accepts contacts; `RecipientStepPayment.tsx` passes `state.email`; insert in [labels/index.ts](supabase/functions/labels/index.ts) is now awaited + logged).
    3. ✅ Lazy-pull tracking path now calls `dispatchNotifications` on status change ([tracking/index.ts](supabase/functions/tracking/index.ts)).
    4. ✅ Webhook handler `webhook_events` insert was using wrong column name (`provider` → `source`); fixed in [webhooks/index.ts](supabase/functions/webhooks/index.ts).
- [x] **Sender email not captured anywhere in the flow** — Added optional sender email field to the "Ship from" section of the Full Label flow ([RecipientStepFullShipping.tsx](src/components/recipient/RecipientStepFullShipping.tsx)); piped through state + `buyLabel` to populate `notification_contacts`.
- [x] **Magic link login doesn't send email** — Root cause: Supabase Auth Site URL was pointed at old Vercel deploy URL. Fixed 2026-03-19: config push to set `sendmo.co`, confirmed John's account, added `detectSessionInUrl` to client.
- [ ] **Full Label flow doesn't create account or link** — After completing the Full Prepaid Label flow, the recipient should have: (1) email verified via OTP, (2) Supabase Auth account auto-created, (3) a `sendmo_links` record in their dashboard. Currently the flow generates a label but doesn't persist the recipient's account or link.
- [ ] **EasyPost test-mode UPSDAP is intermittently flaky** — Discovered 2026-05-19 investigating "only 2 carriers shown" feedback on shipment `9c8fef8d-0a0e-47a6-b260-9096e55068b0`. EasyPost's UPSDAP integration sporadically returns `[UPSDAP] UPS responded with an invalid response, please try again` in test mode, dropping UPS from the rate set on that single call; the next call seconds later for the same route returns UPS normally. Backend logs the carrier_message correctly via `rate.fetched` event_logs. Not a SendMo code bug. Two possible follow-ups: (a) add a one-retry-on-UPSDAP-transient-error path in `supabase/functions/rates/index.ts` if this becomes common in live mode, (b) add a periodic monitor that counts how often UPSDAP appears in `carrier_messages` and alerts if it crosses a threshold. Verify the same flakiness pattern exists in live mode before investing — could be test-mode-only. Reference event log: `rate.fetched` at 2026-05-18 22:53:43 UTC.
- [x] **Real wallet card on Dashboard (after Stripe ships)** — Shipped 2026-05-13 with Stripe Phase B ([proposals/2026-05-13_phase-b-saved-cards-implementation_reviewed-2026-05-13_decided-2026-05-13.md](proposals/2026-05-13_phase-b-saved-cards-implementation_reviewed-2026-05-13_decided-2026-05-13.md)). Saved cards now appear with brand/last4/exp pulled from `payment_methods` rows the `stripe-webhook` function writes on `payment_method.attached`. Add Card / Remove Card flow via a new `/payment-methods` Edge Function (POST creates SetupIntent, DELETE detaches). Mode-aware via the new `profiles.admin_active_mode` column + RPC. SendMo balance UI is still post-MVP (Phase 2/H per master Stripe proposal); this entry only covers the saved-cards half.
- [x] **Edit my label link from Dashboard** — Shipped 2026-04-26 via the Links Manager proposal: dedicated `/links/:id/edit` page (auth-required, prefills from existing `sendmo_links` row) with a Pencil icon button on the Dashboard link card. Backend gained a `PATCH /functions/v1/links/:id` handler with status guards (active/draft only), explicit ownership check (service-role bypasses RLS), and an insert-new-address-row + repoint-FK pattern that preserves historical shipment integrity. Address/preferences form components were extracted into reusable presenters (`AddressForm`, `FlexPreferencesForm`, `LinkShareCard`, `NotificationEmailField`) shared with both `/links/new` and the legacy onboarding wizard. Auth'd users hitting `/onboarding/*` now redirect to `/links/new`.

## Test / CI debt (2026-04-26)

- [ ] **Weekly reconciliation sweep vs Edge wall-clock (added 2026-07-06, T2-1)** — `reconciliation-sweep` `mode=weekly` polls EasyPost Reports up to ~10 min *inside* the Edge Function (`reconciliation-sweep/index.ts` `maxAttempts=20×30s`), which can exceed the function wall-clock limit once real volume makes report generation slow. Registered anyway per John (`reconciliation-sweep-weekly`, Sun 05:00 UTC — it no-ops at current volume and proves the path), but under load: a killed run never advances the weekly cursor (safe/idempotent, next run re-scans), and pg_net fire-and-forget means `cron.job_run_details` can read `succeeded` while the function died — the health signal is `recon_state.reconciliation_weekly.last_run_at` advancing, not job status. Fix when it bites: async report-callback or chunked poll; or unschedule with `SELECT cron.unschedule('reconciliation-sweep-weekly');`. See [proposals/2026-07-06_register-cron-sweeps_reviewed-2026-07-06_decided-2026-07-06.md](proposals/2026-07-06_register-cron-sweeps_reviewed-2026-07-06_decided-2026-07-06.md) (takeover addendum + review N2).
- [x] **Production-verification infrastructure — Layer 1 SendMo port (2026-05-13)** — Cross-project proposal decided 2026-05-13 ([canonical file in agentenvoy/proposals](../agentenvoy/proposals/2026-05-13_claude-production-verification-infra_reviewed-2026-05-13_decided-2026-05-13.md)). **Shipped 2026-05-13:** PLAYBOOK Rule 19 (Browser-verify product-surface fixes, sibling to AgentEnvoy Rule 29) with closed-enum `n/a-category` exemption + `agent-internal` guidance; `Browser-verified:` field documented in LOG.md header conventions; `test:e2e:browser` alias added to `package.json` (existing `test:e2e` preserved); Stop hook at `scripts/claude-hooks/check-browser-verified.sh` + `.claude/settings.json`; slash commands `/runtest`, `/verifyfix`, `/buildtest` at `.claude/commands/`. **Audit findings (not run, documented):** 10 e2e specs exist in `tests/e2e/`; per existing WISHLIST "Test / CI debt" entry, ~14 fail due to missing `VITE_GOOGLE_MAPS_API_KEY`. Suite was not exercised in this pass to avoid burning EasyPost test credits + because the failures are already known + tracked. Future Layer-1.5 work: address Maps-API-key issue and confirm green baseline.
- [ ] **Production-verification infrastructure — Layers 2 + 3** — **Layer 2 (shared Zod schemas)** at SendMo's EasyPost-shaped responses + Stripe-webhook surfaces — likely ~1 day. **Layer 3 (Vercel preview CI** with `VERCEL_AUTOMATION_BYPASS_SECRET` — FREE per the review's correction; **Supabase branching** with documented fallback to dedicated test project per the N2 caveat). Verification driver = Playwright MCP (Claude-Preview = visual-review-only, Claude-in-Chrome out of harness).

- [x] **Edge function imports — migrate off esm.sh + deno.land to JSR** — **✅ DEPLOYED 2026-07-07** via PR [#45](https://github.com/jsa7cornell/Sendmo/pull/45) (squash `bb0226d`); the *Deploy Supabase Edge Functions* job redeployed all functions green in 1m6s, no `522`/graph errors — esm.sh 522 deploy-failure class eliminated (launch-week hardening pickup after esm.sh failed deploys **3× on 2026-07-06** across #41 and #38×2). Migrated to unified exact `jsr:@supabase/supabase-js@2.97.0` (supersedes the draft's `^2.43.0` — matches `package.json`'s `^2.97.0`), `npm:libphonenumber-js@1.13.2`, and `Deno.serve` (deletes the `deno.land/std` dep entirely). 49 URL imports across 35 files → 0. Execution decisions (OQ resolutions, version rationale) in the proposal's 2026-07-06 banner + LOG 2026-07-06/07. Original deferral note kept for provenance:
  Deferred from in-flight proposal [`2026-05-23_edge-function-import-resilience.md`](proposals/2026-05-23_edge-function-import-resilience.md) per John's call 2026-05-23 (OQ1 = defer to post-launch focus window). **Trigger:** 2026-05-23 22:05 UTC deploy failed with `Import 'https://esm.sh/@supabase/supabase-js@2.39.3' failed: 522 <unknown status code>` (Cloudflare 522). Retry succeeded; underlying fragility unfixed. **Scope (full, audited):** 32 files. 21 esm.sh / supabase-js imports (with 3-way version drift `@2`/`@2.39.3`/`@2.43.0`), 1 esm.sh / libphonenumber-js, and `serve` from `deno.land/std@0.168.0` in **every** function (~22 files) — same CDN-outage class, half the problem if only esm.sh is fixed. **Recommended migration:** `jsr:@supabase/supabase-js@^2.43.0`, `npm:libphonenumber-js@1.13.2`, and `Deno.serve` (eliminates the `@std/http` dep entirely). Inline specifiers — no new `deno.json` files needed. **Non-negotiable contract to preserve:** 5 `_shared/` files use `import type { SupabaseClient }` so Vitest's TS transform erases the URL ([_shared/ledger.ts:36](supabase/functions/_shared/ledger.ts:36), [_shared/budget.ts:21](supabase/functions/_shared/budget.ts:21), [_shared/refunds.ts:19](supabase/functions/_shared/refunds.ts:19), [_shared/adjustments.ts:44](supabase/functions/_shared/adjustments.ts:44), [_shared/actor.ts:18](supabase/functions/_shared/actor.ts:18)) — the `type` qualifier must stay or unit tests break. **Why deferred:** H1-H5 launch focus this week; touching every payment-critical edge function during the launch push is asymmetrically expensive vs. the small probability of another esm.sh outage landing on a hotfix. The same probability applies post-launch but in a calmer window and with lower deploy cadence. **When to pick up:** week after launch, or sooner if a second esm.sh failure blocks a hotfix. **Proposal already drafted** — pick up from in-review state; OQ2/OQ3/OQ4/OQ5 still open. ~half day of work + integration test re-run.



- [ ] **Sentry source-map upload (post-launch, fast-follow)** — Deferred from decided proposal [`2026-07-06_sentry-posthog-frontend-monitoring`](proposals/2026-07-06_sentry-posthog-frontend-monitoring_reviewed-2026-07-06_decided-2026-07-06.md) §5. Sentry is wired (T1-3) but stack traces are minified until source maps upload at build time. Needs `@sentry/vite-plugin` in `vite.config.ts` + a `SENTRY_AUTH_TOKEN` build secret in Vercel (kept out of the T1-3 PR to avoid a new build-time secret mid-launch). Release tagging already groups issues per deploy without it. **Pick up when:** the first real production issue is annoying to read minified.

- [ ] **PostHog funnel events + identify() (post-launch, fast-follow)** — Deferred from the same decided proposal (§5, review B4/OQ2). PostHog ships pageview-only (`autocapture: false` — payments PII posture). The fast-follow designs explicit event names for the onboarding funnel + buy conversion and adds `posthog.identify(user.id)` (UUID only, never email) on auth. Needs its own small proposal (event taxonomy is a contract).

- [ ] **Apple Pay domain verification for sendmo.co (post-launch)** — Console warning on flex onboarding: `[Stripe.js] You have not registered or verified the domain, so the following payment methods are not enabled in the Payment Element: apple_pay`. Domain `sendmo.co` is already in Stripe → Settings → Payment Method Domains (Domain ID `pmd_0TYtjsxS6gsndgF3XuyATaLb`, status Enabled), but Apple Pay needs a separate file-verification step on top of generic enablement. Effect: Apple Pay button doesn't render in the Payment Element on supported devices. **Not a launch blocker** — cards work everywhere. Post-launch conversion-lift win for mobile checkout. **Fix path:** (1) In Stripe Dashboard click `sendmo.co` → Apple Pay section → Download `apple-developer-merchantid-domain-association` (no extension). (2) Add to repo at `public/.well-known/apple-developer-merchantid-domain-association` so Vite/Vercel serves it from `https://sendmo.co/.well-known/...`. (3) Verify: `curl -sI https://sendmo.co/.well-known/apple-developer-merchantid-domain-association` → expect `200` + non-HTML content-type (if HTML, the SPA catch-all is intercepting and we need a `vercel.json` carve-out for `/.well-known/*`). (4) Click **Verify** in Stripe Dashboard. (5) Repeat for live mode (separate domain registry in Stripe); one file works for both modes.

- [ ] **Stripe.js Elements API migration to Checkout Sessions (advisory)** — Console warning: `[Stripe.js] Your Elements integration is using an older API. We recommend migrating to the Checkout Sessions API to reduce code, maintenance, and tokens (for AI agents).` Migration guide: https://docs.stripe.com/payments/payment-element/migration-ewcs. Current Elements + SetupIntent integration (`FlexPaymentStep.tsx`, `AddCardModal.tsx`, `RecipientStepPayment.tsx`) is fully supported by Stripe and works correctly — this is a forward-looking nudge. **Not a launch blocker.** Defer until there's a concrete reason (Adaptive Pricing, simpler maintenance, etc).

- [ ] **Clean up ~30 lint errors** — Mostly `@typescript-eslint/no-explicit-any` and `no-unused-vars`, accumulated invisibly while ESLint config was broken. Lint is non-blocking in CI and predeploy.sh until cleaned up. Bulk are mechanical (`unknown` for `any`, `_` prefix for unused vars).
- [ ] **Fix Google Maps autocomplete in CI e2e** — ~14 e2e tests fail because `fillSmartAddress` waits for "Townsend"/"Montgomery" suggestion buttons that never render: `VITE_GOOGLE_MAPS_API_KEY` isn't set in CI, so `@googlemaps/js-api-loader` never loads. Pick one: (a) add a Maps API key to CI env, (b) add a playwright route mock for the Maps script, or (c) refactor tests to use the manual-entry path. E2E is non-blocking in CI until fixed.
- [ ] **Audit + restore the deleted `payments/` edge function dir** — empty directory found at `supabase/functions/payments/`. Either fill it in for the Stripe work or remove the placeholder so predeploy.sh stops skipping it.
- [ ] **Bump `actions/checkout`, `setup-node`, `upload-artifact` to versions on Node 24** — GitHub deprecation warning; runner forces Node 24 by 2026-06-02.

## Data / Reporting

- [x] **Comp labels should show negative margin** — Shipped 2026-05-12 with Stripe Phase A (migration 017). Live Comp shipments now write a `transactions` row of `type='comp_grant'` with negative `amount_cents` (SendMo absorbs the EasyPost cost). The admin report derives margin from the ledger and renders comp shipments with negative margin = the comp_grant amount. See LOG entry "Stripe Phase A — transactions ledger" and proposal [2026-04-26_stripe-integration-plan_reviewed-2026-04-26_decided-2026-05-11.md](proposals/2026-04-26_stripe-integration-plan_reviewed-2026-04-26_decided-2026-05-11.md).

## Features

### Phase 2: Sender Flow & Flexible Link
- [ ] **Sender flow (5-step wizard)** — Build the sender experience at /s/:shortCode: intro, origin + package details, choose shipping method, review + confirm, label ready
- [ ] **Seller Marketplace Link (buyer-pays variant)** — A second flavor of flex link, inverse of today's recipient-prepaid Flexible Link. **Use case:** a seller posting on Facebook Marketplace / Craigslist / eBay needs a link they can paste into their listing. The buyer clicks the link, enters their ship-to address, picks a shipping option, and pays at checkout — standard e-commerce flow. The seller then prints the prepaid label from their dashboard and ships. **Why we need it:** today's Flexible Link is recipient-driven (recipient prepays, sender prints), so the Marketplace tip card on `/links/new` (shipped 2026-05-18) points buyers at the recipient-prepaid flow, which is the wrong flow for sellers. **Shape:**
    1. Seller creates the link from their dashboard: enters their *origin* address + uses the existing FlexPreferencesForm (speed, carrier, optional price cap). No prepayment required. Link is `active` immediately. New `link_type='seller_marketplace'` (or similar) on `sendmo_links`.
    2. Buyer hits `/s/:shortCode`, sees a short intro ("Shipping with SendMo — enter your address to get a label"), inputs destination address + package size hint, picks a rate from the existing rate picker, and pays via Stripe checkout (Payment Element, not the saved-card off-session pattern — this buyer has no prior relationship to SendMo).
    3. On payment success: EasyPost buy → label PDF delivered to seller (email + their dashboard); buyer gets a tracking confirmation email + receipt; seller marks "shipped" once they hand off to carrier (or auto-flips on first carrier scan).
    4. Optional seller toggle: "I'll cover shipping" (sets price_cap, eats the cost) vs. "Buyer pays" (default). Both flows reuse the same buyer checkout; only who pays at the end differs.
- [ ] **Marketplace snippet — wire to the right link variant** — Once Seller Marketplace Link ships, the `/links/new` post-create Marketplace tip card needs to be re-pointed. Today (2026-05-18) it surfaces the recipient-prepaid short link, which is the wrong flow for someone *selling* on Marketplace. Two paths: (a) only show the Marketplace tip on Seller Marketplace Link creation, hide it on the recipient-prepaid path; (b) on the recipient-prepaid path, replace the tip with a "Buying on Marketplace? Send your link to the seller" framing. Likely both.
- [ ] **Real shipping insurance build** — Today the package-step Insurance toggle is **hidden** (2026-05-24) because it was phantom: it collected $2.50 from customers but never passed `insurance` to EasyPost's `/buy` endpoint. Real insurance build requires: (1) declared-value field on the package step (Magic Guestimator pre-fills); (2) insurance toggle gated on declared value > 0; (3) premium computed live; (4) EasyPost `/buy` actually passes `insurance` parameter; (5) `shipments.{declared_value_cents, insurance_cost_cents, insurance_easypost_id}` columns persisted; (6) claims pipe v1 = `mailto:claims@sendmo.co`. **John's preferred pricing was $3 + 0.5% but that crosses into loss at ~$600 declared value — use $3 + 1.5% (always nets ≥0.5% above EasyPost's 1%).** **Hard gate before charging real money:** confirm with EasyPost that their TOS Master Customer Agreement allows insurance markup (their Shipping Insurance Addendum isn't publicly posted; 10-min email to your EasyPost account contact). Full product brief: [`previews/insurance-product-brief.html`](previews/insurance-product-brief.html). Implementation design: [`proposals/2026-05-12_shipping-insurance.md`](proposals/2026-05-12_shipping-insurance.md). **Priority:** below Seller Marketplace Link per John 2026-05-24.

### Phase 3+: Premium Features & Analytics
- [ ] **Saved addresses / locations** — Let users store their frequently-used addresses for quick re-use in future shipments. Scoped to authenticated users.
- [ ] **Reusable templates** — Allow recipients to save link preferences (distance, size hint, speed tier) as reusable templates. Pre-populate future links with a single tap.
- [ ] **Branded ship-to links** — Custom domains or subdomain support (e.g., company.sendmo.co/join) so links feel more professional/personal to the recipient's brand.
- [ ] **Shipment tracking dashboard** — Real-time tracking dashboard for the recipient to monitor all packages being shipped to them. Shows status, ETA, carrier, last update.
- [ ] **Resend link for repeats** — Quick "Send again" button that lets the recipient re-send their link to a new sender without re-entering all preferences.
- [ ] **Analytics: viewed, completed, shipped** — Track link analytics (views, completion rate, shipments, total $ received). Accessible from Dashboard.

### Existing Features (In Progress)
- [ ] **Failure-mode tracking emails — void/refund, return_to_sender, exceptions** — Today's notification pipeline only fires emails for the happy-path statuses (`in_transit`, `out_for_delivery`, `delivered`). Silent failure modes that the user never hears about: (1) **Label voided / refund processed** — recipient was told the package is on its way (label-confirmation email already sent), then the label is voided; they never learn it's not coming. Send a void email to both sender + recipient when `refund_status` transitions to `submitted` or `refunded`. (2) **Return to sender** — `shipments.status = 'return_to_sender'`, currently a silent DB state. Send an "Your package is being returned" email with the reason from the latest tracking event if available. (3) **Exceptions** — EasyPost surfaces non-terminal exception events (address invalid, delivery attempted, held at facility). Decide whether to forward as a tracking-update email or batch into a single "Delivery exception" email. Implementation: extend `NOTIFY_STATUSES` in [tracking/index.ts](supabase/functions/tracking/index.ts) + [webhooks/index.ts](supabase/functions/webhooks/index.ts); add new template functions in [_shared/email-templates.ts](supabase/functions/_shared/email-templates.ts); wire void emails to the existing refund path in [refundService.ts](src/lib/refundService.ts). Surfaced during the 2026-05-11 email audit.
- [ ] **Retry transient Resend failures** — `notifications_log` rows can land at `status='failed'` if Resend returns 5xx or times out, and the next webhook on the same shipment for a *later* status won't backfill the missed earlier one (idempotency check is per-event-type). Add a retry worker — either an Edge Function on a cron, or a Supabase scheduled trigger that re-attempts `status='failed'` rows < 24h old with exponential backoff. Cap retries at 3. Surfaced during the 2026-05-11 email audit.
- [ ] **Cache `tracking_details` events in DB** — Tracking page now always fetches live from EasyPost on every load (no TTL cache; reads are free), so this is lower priority. Worth doing eventually so the events list still renders if EasyPost ever goes down.
- [ ] **Register EasyPost webhook URL** — Configure EasyPost dashboard to send `tracker.updated` events to `https://fkxykvzsqdjzhurntgah.supabase.co/functions/v1/webhooks`. The webhook handler is already built and deployed — just needs the URL registered. This will give us real-time status updates pushed by EasyPost instead of polling on page load. Currently we use a 30-min TTL cache on the tracking function; webhooks would make this near-instant and eliminate API polling entirely.
- [ ] **Cache tracking events in DB** — Store EasyPost tracking events (message, status, datetime, location) in a `tracking_events` table so the tracking page can show event history even when serving from cache (within the 30-min TTL). Currently events only show when a fresh EasyPost fetch happens.
- [ ] **Payment transaction ledger** — Per CLAUDE.md rule 16, financial balance changes must use an append-only ledger table. Create a `transactions` table: `id, user_id, shipment_id, type ('charge'|'refund'|'credit'|'fee'), amount_cents, stripe_ref, description, created_at`. All money movement flows through this table. Required before Stripe goes live.
- [ ] **AI-powered FAQ / Help center** — Replace the static FAQ page with a smart help experience. Include: (1) AI-based search that answers questions from a knowledge base (shipping policies, pricing, how-it-works, troubleshooting), (2) a curated list of real questions people actually ask (sourced from support emails, user testing, etc.), and (3) unified FAQ + Help in one place (accessible from header for both signed-in and signed-out users). Consider embedding an AI chat widget or search-first UX.
- [ ] **Flexible label generation** — After the sender selects a shipping rate on the flex link flow, generate and display the actual shipping label (PDF). Requires: Stripe payment capture on the flex link's authorized hold, EasyPost label buy call with the selected rate, label PDF storage in Supabase Storage, and a download/print UI on the done step. Currently the done step shows a placeholder ("Label generation coming soon").
- [x] **Stripe refund on label void** — *Fully shipped across H1–H5.* Two-step refund: cancel-label submits EP void, tracking poll + webhooks fire Stripe createRefund when EP confirms, stripe-webhook writes the ledger row. Admin `/refunds` tool for goodwill partial refunds (H3). Three lifecycle emails (A/B/C) and the 21-day cron sweep (H5) close the remaining gaps. Only outstanding fast-follow: enable pg_cron to run the sweep automatically. See LOG entries 2026-05-13 (architecture), 2026-05-23 (H3 H5 completion).
- [ ] **EasyPost refund webhook wiring** — Verify exact event names against EasyPost's webhook docs (probably `refund.successful` / `refund.rejected` or bundled into `tracker.updated` with a refund-status field), then add a case to `webhooks/index.ts` to handle them. When the event fires for a shipment in `refund_status='submitted'`: same two-step logic as the tracking-poll path — call Stripe `createRefund` if PI present, mark `not_applicable` if comp, mark `rejected` if EP rejected. Cuts latency from "next page view after carrier confirms" to "seconds after carrier confirms." Won't matter until Phase E produces Stripe-paid cancels; lazy poll covers today.
- [x] **Cron-poll for stale `refund_status='submitted'` shipments** — *Shipped 2026-05-23 (H5).* `cron-refund-sweep` edge function (21-day threshold per Decision D3) handles this. Data layer + migration 035 are live. **Cron registration is the fast-follow**: enable `pg_cron` + `pg_net` extensions, then apply migration 035 Block 2 to register the daily 04:30 UTC schedule. Until then, admin trigger via AdminReconciliation → "Rejected refunds" tab.
- [ ] **User payment history page** — Show all transactions (charges, refunds, credits) for the authenticated user. Read from the `transactions` ledger table. Accessible from Dashboard.

---

*Last updated: 2026-05-11 — added failure-mode tracking emails + transient-Resend retry. Surfaced during the email audit that also produced PR #31 (delivery-performance badge) and the public-tracking-code proposal.*

---

## Added 2026-05-18 — Pattern D follow-ups

Items deferred from the Pattern D execution proposal (`proposals/2026-05-16_flex-payment-pattern-d-execution_reviewed-2026-05-16_decided-2026-05-18.md`) §5 "Out of scope":

- [ ] **ZDA verification at SetupIntent save (Pattern D')** — Add a small `$0` (Stripe ZDA, falls back to $1) auth-and-void server-side immediately after `setup_intent.succeeded` fires. The research proposal recommended this on first-pass for the "your card was accepted" moment of recipient confidence. John (2026-05-16) directed strict Pattern D first so we can measure decline rates via `stripe_intents.last_payment_error_code`. Add this if 4 weeks of telemetry shows real card-validity issues post-SetupIntent that the ZDA would have caught.
- [ ] **Nightly background PM validation cron** — Once per day, validate every active flex link's default PM via a $0/$1 auth+void. Catches replaced/cancelled cards before a real sender hits the failure. Pair with the existing decline-recovery email. Defer until scale demands.
- [ ] **30-day card-expiry warning email** — When a recipient's default PM is within 30 days of `exp_year`/`exp_month`, send a heads-up email with the "update card" CTA. Catches the "I haven't shipped in 6 months and now my card's about to expire" case.
- [ ] **LinksEditor `/links/new` payment validation integration** — The dashboard "+ New Link" path currently creates flex links without ever collecting a PM. Under Pattern D, the link is created `active` but `is_funded=false` until the recipient adds a card via Dashboard. UX is fine but two-step. Could pull the SetupIntent flow into `/links/new` so card collection happens inline at link creation.
- [ ] **Sender self-paid fallback flow** — When a flex link is Inactive at the front gate, today's UX is "check back with the recipient." Add an alternative: offer the sender a self-paid label flow with the recipient's address pre-filled. Sender pays for the shipment themselves. Requires its own UX + payment surface.
- [ ] **Multi-PM retry on off_session decline** — If the recipient has multiple saved PMs and the default declines, the labels Edge Function could try the next PM (`is_default DESC, created_at DESC`) before flipping the link Inactive. Matches Stripe's Smart Retries pattern.
- [ ] **SCA / `requires_action` recovery flow** — Pattern D treats `requires_action` (EU/UK 3DS-required) as a decline in v1. For when SendMo expands to EU: implement a recovery flow that emails the recipient with a Stripe-hosted 3DS challenge link.
- [ ] **Background-job worker for webhook side-effects** — Today the `stripe-webhook` sends the decline-recovery email inline (5s timeout, `event_logs` fallback on failure). At higher scale, move email-send out of the webhook critical path via a separate worker / cron / edge-job.
- [ ] **`sendmo_links.status` enum cleanup** — Drop `'in_use'`, `'used'`, `'completed'` values from the CHECK constraint via a future migration. Pattern D doesn't write them anymore (backfilled in migration 024); they're dead values cluttering the enum.
- [ ] **Fraud mitigation escalation** — Pattern D ships a 5/60s per-(IP, short_code) rate limit on the labels Edge Function's flex path. If decline-burst patterns surface in `link_state_events.charge_failed` rows, escalate to: Stripe Radar integration, per-customer cumulative caps, soft-lock-and-recipient-acknowledge after N failed gates in M min.
- [ ] **Drop the dead `'used'` legacy check in `links/index.ts:66`** — migration 020 renamed the status value `'used' → 'in_use'`, but the link resolver still has a vestigial `if (link.status === "used" && link.link_type !== "full_label") return 410` check that's now dead code. Cleanup task.

---

## Added 2026-05-19 — Confirmation/tracking unification follow-ups

Items deferred from the [unify-confirmation-into-tracking proposal](proposals/2026-05-19_unify-confirmation-into-tracking.md). v1 ships without these; revisit if usage shows they matter.

- [ ] **"Email me the receipt" modal for anonymous viewers** — Today anonymous viewers on `/t/<code>` see no payment info at all (payers reach receipts via Dashboard sign-in). For anonymous viewers who *are* the payer but landed on the URL without auth signals, expose a "View receipt" link that opens a modal with an "Email receipt to me" form. Server-side check: requested email must match the payer's on-file email; on match, email the receipt PDF via Resend. Mirrors the existing "Resend tracking email" pattern. Punted from v1 because the "go sign in via Dashboard" fallback is sufficient and this adds a new edge function + UX surface for a small audience.
- [ ] **Real support intake system** — Today the delivered-state "Everything OK?" card uses `mailto:support@sendmo.co` with a pre-filled subject + shipment context. This is fine for v1 (no infra dependency, support email already in use per Terms.tsx), but doesn't scale: no ticket tracking, no template responses, no SLA visibility. Upgrade options: (a) Zendesk/Help Scout via existing Resend transactional infrastructure, (b) Linear "Issue" creation via API for engineering-routable bugs, (c) inline form that POSTs to a new edge function + writes to a `support_tickets` table. Pick when support volume justifies it.
- [ ] **Scheduled-pickup detection + alternative pre-drop-off hero** — Today SendMo's full-prepaid + flex flows both assume the shipper physically drops the package off (`HowToShipStrip` step 3, `dropOffCopy()` helper, etc.). USPS, UPS, and FedEx all expose pickup scheduling via the EasyPost Pickup API; some users also have package-pickup arranged separately (USPS daily mail carrier pickup, scheduled UPS/FedEx pickup at a billed rate, recurring building pickup). When pickup is the path, the pre-drop-off hero + ETA + HowToShipStrip should adapt: the "person walking with package" illustration becomes a "carrier arriving at your door" illustration, the ETA copy switches from "Drop off today → arrives X" to "Pickup scheduled Wed → arrives X", and HowToShipStrip step 3 ("Drop off") becomes "Leave for pickup." Requires (a) EasyPost Pickup API integration in a new edge function, (b) a `shipments.pickup_*` column set (scheduled_at, location, easypost_pickup_id), (c) UI conditioning across all post-purchase surfaces. Punted from the unify-confirmation-into-tracking v1 because today there's nothing to detect — v1 ships drop-off-only.

---

## Added 2026-05-20 — Brain-level infrastructure

> Not SendMo-specific — filed here because there's no brain-level wishlist today. Move to `~/AI-Brain/WISHLIST.md` if one is ever created.

- [ ] **Version-control the `~/AI-Brain/` brain docs** — The brain-level docs at `~/AI-Brain/` (`GLOBAL-PLAYBOOK.md`, `CLAUDE.md`, `USER.md`, `REGISTRY.md`, `PROPOSAL-REVIEW-PROTOCOL.md`, `How Johns AI Brain Works.md`) govern every agent in every project and are edited by agents (session-wrap + doc-update protocols) — yet `~/AI-Brain/` is **not a git repo**, so they have zero version history, no undo, and no backup, while every project they govern *is* on GitHub. The manual "*Last updated: …*" footers in those files are hand-rolled version control. Surfaced 2026-05-20 in a SendMo session while discussing where to durably record a concurrent-agent worktree-base rule — discovered `GLOBAL-PLAYBOOK.md` isn't version-controlled at all. **Recommended approach:** `git init` at `~/AI-Brain/`, local-only to start — gives history / `git diff` / `git blame` / undo with zero secret-exposure risk; a private remote for offsite backup is an optional second step. **Critical gotchas:** (1) `~/AI-Brain/` holds 21 subdirectories — 4 are their own repos (`College`, `jot`, `sendmo`, `wind`), the rest are project/working folders — none of which belong in a brain-docs repo. Use an **allowlist `.gitignore`**: ignore everything by default (`/*`), then explicitly un-ignore only the brain docs (`!/GLOBAL-PLAYBOOK.md`, etc.). "Deny by default, allow the docs" — so new project folders and any secrets are excluded automatically. (2) **`SECRETS-MAP.md`** is a credential-location inventory — leave it untracked; never commit it, even to a private repo. (3) **Do not move the files** — every project's `CLAUDE.md` references `../GLOBAL-PLAYBOOK.md` etc. by relative path; moving them breaks the harness read-order. Wrap a repo around them in place.

---

## Added 2026-05-22 — from payments risk-intelligence proposal

> See [proposals/2026-05-21_payments-risk-intelligence_reviewed-2026-05-22_decided-2026-05-22.md](proposals/2026-05-21_payments-risk-intelligence_reviewed-2026-05-22_decided-2026-05-22.md).

- [ ] **Turnstile (CAPTCHA) on account signup** — Cloudflare Turnstile on the Supabase Auth OTP/signup path as a bot-farm speed bump. **Parked here 2026-05-22:** John does not want signup friction, and per the risk-intelligence proposal §4.4 the load-bearing bot-farm defense is making a farmed account *unable to move money* (Radar at PM-add + the velocity hierarchy), **not** blocking signups. Turnstile is free and mostly invisible (managed mode), and Supabase Auth has native Turnstile support — a config flip + a widget, not a vendor integration. Revisit if real bot-farm activity shows up in `velocity.limit_hit` or signup telemetry.

---

## Added 2026-05-24 — multi-card lifecycle (cross-PM cancellation safety)

> Surfaced during John's first live label-buy + cancel test. Refunds-on-cancel already work correctly today (Stripe routes refunds to the original charge's PM automatically regardless of current Customer state — verified semantics, not just hope). The real architectural gap is on **carrier-adjustment auto-recharge** + **user-side card deletion**.

- [ ] **Multi-card lifecycle: charge adjustments to original-shipment PM + soft-detach on user-side delete** — Two related changes that protect against a user-side card change between buy-time and a post-pickup carrier adjustment.

  **(1) Adjustment recharges target the original shipment's PM, not the user's current default.** H2's `_shared/adjustments.ts:resolveRecovery` today reads `payment_methods` for the user's current default and charges that. If the user has added a new card and demoted the old one between buy-time and a carrier reweigh, the recharge would hit the NEW card — architecturally wrong (the adjustment is correction-of-original-shipment) and user-confusing. Fix: denormalize `payment_method_id` onto `shipments` at buy time (sourced from the PI's payment method via `stripe_intents.payment_method_id`, which we already have). `resolveRecovery` then prefers `shipment.original_pm_id` and only falls back to the current default if the original PM is no longer attached (e.g. bank closed the card).

  **(2) `DELETE /payment-methods/:id` soft-demotes, not detaches.** Today the endpoint calls Stripe's `detach` immediately, which fully removes the PM from the Customer. That makes the original card unavailable for both (a) the adjustment recharge above and (b) the My Wallet "card on file" display next to historical receipts. New behavior: set `is_default=false` locally (demote), keep the PM attached to the Stripe Customer. Stripe Customers naturally hold multiple PMs simultaneously — this just stops actively detaching them. Real detach can be reserved for an explicit "remove permanently" admin action (or a periodic sweep that detaches PMs with no shipments referencing them in the last 90 days).

  **Why not urgent:** refund-on-cancel routing already works (Stripe handles it). The adjustment-recharge concern requires (a) a carrier-adjustment fires AND (b) the user changes their card between buy-time and the reweigh — both rare alone, vanishingly rare combined at launch volume. Defer to a focused post-launch proposal once real-world data clarifies the size of the gap.

  **Files involved:** `_shared/adjustments.ts` (resolveRecovery PM-selection logic), `payment-methods/index.ts` (DELETE handler), `shipments` schema (new `original_payment_method_id` column via migration), `payments/index.ts` + `labels/index.ts` (capture + persist `original_payment_method_id` at buy time).

  Surfaced by John 2026-05-24 during the first live label-buy + cancel test.

- [ ] **Link-locked payment method (Option A) — pick once at link creation** — Today the link's `isFunded` calc looks for the user's *default* PM in the local `payment_methods` table. That has two failure modes already hit: (a) Stripe doesn't auto-promote new PMs to default, so a newly-attached card leaves the link "Inactive" until something explicitly sets default; (b) the "Add payment information" CTA on the dashboard link card opens a fresh new-card flow with no option to pick from saved cards — surprising when the user already has cards on file. **New model:** `sendmo_links.payment_method_id` (FK to `payment_methods.id`), required at link creation. UX flow: `/links/new` shows a saved-cards picker as a step + "+ Add new card" option; user picks → link is permanently bound to that PM. Dashboard link card displays "Charges to **Amex •••• 5001**" instead of an Add/Update button. Server-side `labels/index.ts` flex charge queries `link.payment_method_id` directly (not "default PM"). When the linked PM expires or is removed: link auto-deactivates → "Reactivate with another card" CTA opens the picker again. **Why not urgent:** the immediate live-test issue is solved by backfilling the local `payment_methods` row + setting Stripe's `invoice_settings.default_payment_method` (today). Real link-locked PM model is a 1-day post-launch project. **Decision made by John 2026-05-24:** Option A (no edit-card escape hatch on dashboard; force full re-link if user wants a different card — "wait til people complain about it"). **Files involved:** `sendmo_links` schema (new `payment_method_id` column + FK + NOT NULL constraint after backfill), `links/index.ts` (new model in resolver), `labels/index.ts` (PM source for off-session charge), `Dashboard.tsx` (link card display), `/links/new` flow (saved-cards picker step), `Admin.tsx` (admin still needs to see what PM is bound to each link). Surfaced by John 2026-05-24 from the dashboard "My Label Link / Add payment information" surface during launch testing.

- [ ] **Adaptive 3DS — find/document the toggle, decide whether to enable** — Stripe Radar's Risk Controls page lists Adaptive 3DS as Inactive but John couldn't find the toggle to flip it on. May have been moved/renamed by Stripe (their Risk Controls UI consolidated under "Improved risk protection" auto-management 2024). Investigate: (a) is the toggle present and we missed it, (b) is it auto-managed now per the "managed threshold" banner, or (c) does it require Radar for Fraud Teams (the paid tier)? Once located, decide whether to enable (low-friction ML-driven 3DS challenge only for risky payments — recommended). Not launch-blocking; built-in Radar already blocking at risk_level=highest covers the major attack vectors. Surfaced 2026-05-24 during B1 launch config.

- [ ] **`refund_fee_recovered` ledger writer — Stripe fee partial recovery on refund** — When a Stripe charge is fully refunded, Stripe typically refunds the **fixed $0.30** portion of the original fee (but keeps the 2.9% variable). Today `stripe-webhook` writes `fee_stripe` on `charge.succeeded` but does NOT write a `refund_fee_recovered` row on `charge.refunded`. Net effect: SendMo's net-margin display overstates the fee cost by ~$0.30 for every refunded shipment (small but accumulates). The reconciliation proposal admits the `refund_fee_recovered` type but no writer is wired. Fix: in `stripe-webhook` `charge.refunded` arm, retrieve the refund object's `balance_transaction` (expand on the refund), read `fee_amount` (negative — Stripe's portion returned to us), write a `refund_fee_recovered` row with positive `amount_cents`. Surfaced 2026-05-24 on YPPY9AK: fee_stripe -$0.57 stuck on the books even after full $9.18 refund. Verified observable. Mostly a reconciliation-dashboard accuracy concern; not customer-facing.

- [ ] **`easypost_refund` row written at $0 when EP payload's refund amount can't be sourced** — H1 writer at `webhooks/index.ts` falls back to `$0` and idempotency key `shp_fallback_<eps_id>` when the EasyPost refund.successful payload doesn't expose `result.refunds[0].amount` or `result.refund.amount`. Observed on YPPY9AK 2026-05-24: $7.11 UPS wallet credit landed but our ledger has `easypost_refund +$0` — the Reconciliation dashboard "Refund from EasyPost" column displays $0 incorrectly. Backfill needed for affected rows. Going-forward fix: when the payload shape is the Refund object directly (shape c — `result.shipment_id` is set, `result.amount` is the refund amount in dollars), source from `result.amount` directly. The 2026-05-24 webhook-shape fix (`076bf75`) handled `epShipmentId` resolution but didn't extend to amount sourcing. Cross-link to multi-card lifecycle entry above — same area of code.

- [ ] **Full-label statement-descriptor suffix: per-shipment code, not "LABEL"** — Today `payments/index.ts:372` hardcodes `statement_descriptor_suffix: "LABEL"` for every full-label PI, so all full-label charges show "SENDMO.CO* LABEL" on bank statements — no way to tie a specific bank entry back to a specific shipment for customer support inquiries. Root cause: the PI is created at /pay step BEFORE the EasyPost label is bought, so `shipments.public_code` doesn't exist yet at PI creation time. Two fix options: (a) **Update after the fact** — after `admin_insert_shipment` returns with the public_code, call Stripe's `POST /v1/payment_intents/<id>` to update `statement_descriptor_suffix`. Stripe allows updates pre-capture; the auto-capture flow would need to update between PI creation and confirmation, OR use a brief manual-capture window. (b) **Restructure order** — create the shipments row (with public_code) before the PI, then create the PI with the suffix already set. Bigger change. Recommend (a). Flex + adjustment-recharge paths already do this right (link short_code + shipment public_code respectively); only the full-label hardcode needs fixing. Surfaced 2026-05-27 during the business-identifier sweep. Not customer-blocking but real support-traceability gap once you have real customer volume.
