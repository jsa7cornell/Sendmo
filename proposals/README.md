# SendMo proposals

See `~/AI Brain/PROPOSAL-REVIEW-PROTOCOL.md` for the protocol.

## Active proposals

- [2026-05-22_reconciliation-and-carrier-adjustments_reviewed-2026-05-22_decided-2026-05-22.md](2026-05-22_reconciliation-and-carrier-adjustments_reviewed-2026-05-22_decided-2026-05-22.md) — **launch-blocker** reconciliation + carrier-adjustment system. Implements master plan [`§3.7`](2026-04-26_stripe-integration-plan_reviewed-2026-04-26_decided-2026-05-11.md) (originally Phase G post-MVP; promoted to launch blocker by John 2026-05-22 after the comp-dogfooding leak finding — 4 cancelled comp labels with no record of wallet credit). Two corrections to §3.7: detection is via the **`shipment.invoice.created`/`updated`** webhook (verified at docs.easypost.com/docs/events; §3.7's `tracker.updated`/`shipment.updated` was drift); floor is **$1** with a **$1 handling fee** (vs. $2, no fee). Reviewed approve-with-changes (4 blockers — all accepted): B1 — full-label saves no card today (`payments/index.ts:208` — no `customer`, no `setup_future_usage`), so the auto-recharge tier was structurally unreachable for the dominant flow; B2 — `carrier_adjustments` already exists in migration 017 with a different shape (`ALTER` not `CREATE`, partial UNIQUE on `source_event_id` missing); B3 — `transactions.type` CHECK rejects `label_cost`/`easypost_refund` (hard prerequisite ALTER); B4 — `easypost_refund` idempotency key must key on the EasyPost Refund object id, not shipment id. **DECIDED 2026-05-22:** **D1** — option (a) on B1: extend `payments/index.ts` to save the card at full-label checkout (Stripe Customer + PM + `setup_future_usage: 'off_session'` + brief checkout consent disclosure) — leverages decided Phase B saved-cards groundwork; brings auto-recharge to parity across both flows from day one. **D2** — all technical fixes folded into §3 (migration 032 covering both ALTERs + the UNIQUE + the `expected_credit_cents` column for N4's dispute-pattern-match; per-refund-object keying; `SELECT FOR UPDATE` on cap-math; sweep drift-detection; dispute-window countdown on flagged items; explicit email send-site). **D3** — entire bundle is P1 (launch blocker per John); the comp-dogfooding phase can begin once detection + ledger + reconciliation land. Builds: bidirectional `transactions` ledger (amends PLAYBOOK Rule 16 writer map); `carrier_adjustments` ALTER (weight cols + `expected_credit_cents` + UNIQUE source_event_id + `rejected` value); shipment.invoice webhook handler + tiered recovery (≤$1 absorb · $1–$10 auto-recharge with $1 fee · >$10 flag-for-you · §3.7 caps preserved); full-label save-card extension; `reconciliation-sweep` Edge Function (daily incremental + weekly bulk via Reports API — `payment_log.delta_fee` backstop); admin Reconciliation dashboard ([mockups: dashboard](../previews/reconciliation-dashboard.html), [detail view](../previews/shipment-detail.html)); chargeback column + Needs-Attention with evidence deadline. Prepaid-balance model deferred to WISHLIST. **Status:** decided, implementation starting.
- [2026-05-21_refund-system-implementation_reviewed-2026-05-21_decided-2026-05-22.md](2026-05-21_refund-system-implementation_reviewed-2026-05-21_decided-2026-05-22.md) — implementation of the decided refund-flow review (admin `/refunds` Edge Function, partial-refund plumbing, denied state, three lifecycle emails, `charge.refund.updated` failure detection). Reviewed approve-with-changes (3 blockers — all accepted): per-PI balance helper (B1), `RefundRequest` keyed off the charge ledger row to give per-PI scoping for free (B2), EasyPost has no `refund.rejected` webhook so rejection detection is poll + cron only (B3). **DECIDED 2026-05-22:** failure-detection (`charge.refund.updated`) pulled into Phase 1 — data-model focus, no customer email; admin `/refunds` refunds send no customer email; cron sweep stays Phase 2 with 3-week threshold; terminal "denied" reuses `refund_status='rejected'` (customer word "Refund unsuccessful"); three lifecycle emails approved (carrier-aware copy, name canceller only when not the payer). P1 = `/refunds` tool + partial plumbing + failure-detection handler — go-live blocker; P2 = emails + cron + admin rejected queue. **Status:** decided, implementation starting.
- [2026-05-21_refund-flow-review_reviewed-2026-05-21_decided-2026-05-21.md](2026-05-21_refund-flow-review_reviewed-2026-05-21_decided-2026-05-21.md) — design review of the two-step refund (cancel a label → carrier void → wait for carrier confirmation → only then refund the customer's card). Triggered by Job 3 go-live: the refund path had never run against a real charge, and the two-step shape was a 2026-05-13 LOG-only drift from [`2026-05-11_label-cancel-and-change`](2026-05-11_label-cancel-and-change_reviewed-2026-05-12_decided-2026-05-12.md) §2.3, never proposal-reviewed. Reviewed approve-with-changes (3 blockers — all accepted; the EasyPost-gated two-step John chose dissolves the worst two). **DECIDED 2026-05-21:** keep the **two-step / EasyPost-gated** refund (proposal's recommended Option B was rejected — the review showed it under-priced its loss + had an inoperable safety valve). Decisions: (1) customer refunded only after EasyPost credits SendMo; (2) third terminal `rejected` ("refund denied") state formalized end-to-end with reason capture; (3) refunds partial-capable by design; (4) admin `/refunds` tool is a **go-live blocker**; (5) three lifecycle emails (submitted / completed / denied); (6) invalid-PM refund failure = documented edge case, manual resolution but automated detection. Cross-links WISHLIST #79 + #87–89 and [`2026-05-21_payments-risk-intelligence`](2026-05-21_payments-risk-intelligence.md). **Status:** decided — next artifact is a follow-up *implementation* proposal (the `/refunds` function, partial-refund plumbing, webhook `refund.rejected` arm, the three emails, failure-detection alert, admin queue) which itself goes through the protocol before code lands.
- [2026-05-13 · Claude production-verification infrastructure (cross-project; canonical file in AgentEnvoy proposals)](../../agentenvoy/proposals/2026-05-13_claude-production-verification-infra_reviewed-2026-05-13_decided-2026-05-13.md) — **DECIDED 2026-05-13** (approve-with-changes) · canonical file lives at `agentenvoy/proposals/2026-05-13_claude-production-verification-infra.md`; this is the SendMo-side pointer. SendMo already has Playwright installed (`playwright.config.ts`, `test:e2e`); Layer 1 work for SendMo is an audit pass (is the existing suite passing? wired into CI? add Playwright MCP). No file changes in SendMo for Layer 1. Layers 2 (shared Zod schemas) and 3 (Vercel preview CI + Supabase branching) follow in separate implementation passes. Lands as WISHLIST infrastructure entries on both projects once decided.
- [2026-05-13_phase-b-saved-cards-implementation_reviewed-2026-05-13_decided-2026-05-13.md](2026-05-13_phase-b-saved-cards-implementation_reviewed-2026-05-13_decided-2026-05-13.md) — Phase B of the Stripe roadmap (master proposal §6 row B): saved cards on file, SetupIntent + Stripe Customer object, Dashboard wallet UI replacing the "Coming Soon" placeholder. No charging — Phase C lands that. Reviewed approve-with-changes; 4 blocking findings (webhook primary/secondary flip — `payment_method.attached` is row-writer not `setup_intent.succeeded`; client-supplied mode killed in favor of new `profiles.admin_active_mode` column + RPC per Rule 14; explicit `config.toml` entry for `verify_jwt`; migration 022 ships indexes on `stripe_customer_id_<mode>`) all accepted. T1 decided 2026-05-13: global 3-mode admin toolbar lives in `AppHeader.tsx` to the left of the user/login section, replacing the `RecipientOnboarding.tsx`-local toolbar; mode reads from `profile.admin_active_mode`, sets via `set_admin_active_mode()` RPC. **Status:** decided 2026-05-13, implementation starting. WISHLIST item "Real wallet card on Dashboard" closes when Phase B ships.
- [2026-05-13_tracking-page-ia-polish_reviewed-2026-05-13_decided-2026-05-13.md](2026-05-13_tracking-page-ia-polish_reviewed-2026-05-13_decided-2026-05-13.md) — IA polish on `/t/<public_code>` after John's 2026-05-13 dogfood. Pivots from today's single-skeleton-with-toggles to per-family composition: F1 Ready-to-Ship (Print/HowToShip/DetailsCard+Cancel), F2 In-Motion (horizontal progress + ActivityFeed), F3 Cancelled (banner + Details + PrintAnotherLabel). Adds Phase 2 print logging — new `POST /label-print` edge function writes `label.printed` event_logs rows with actor/user_id/ip/user_agent/session_id to resolve the two-viewer "who printed it" disambiguation; tracking GET returns `print_count`. Renames page "Code" → "SendMo ID"; adds Item + From/To + carrier-adjustment stub line. Ships admin-affordance footer (gated on `isAdmin`) deep-linking to `/admin?shipment=<id>` — full inline admin panel is Ask 4 (separate proposal). Reconciles with sender-flow-wizard Round 2 (the /t/ promotion), label-cancel-and-change (reuses 3-path auth shape), sendmo-public-tracking-code (canonical ID), stripe-integration-plan §11 #5 (Phase G carrier-adjustment slot). **Status:** decided 2026-05-13 (T1=a / T2=i — item_description ships in this PR via migration 021 with anonymous-allowed visibility); implementation starting.
- [2026-05-12_shipping-insurance.md](2026-05-12_shipping-insurance.md) — replace the dummy "+$2.50 covers up to $100" toggle (which today is a phantom charge — we never pass `insurance` to EasyPost's `/buy`) with a real product: a `declared_value` field on the package step (Magic Guestimator pre-fills it), insurance toggle gated by non-zero declared value, premium computed live, EasyPost `/buy` actually binds coverage via the `insurance` parameter, and `shipments.{declared_value_cents, insurance_cost_cents, insurance_easypost_id}` persist for claims later. Pricing: John asked for `$3 + 0.5%`; that crosses into loss at ~$600 declared value, so author proposes raising to `$3 + 1.5%` (always nets ≥0.5% above EasyPost's 1%) — flagged as OQ#1. Claims pipe v1 is `mailto:claims@sendmo.co` with John triaging; disclosure copy names EasyPost as the coverage provider and SendMo as facilitator. Legal-eagle review on insurance-reseller posture (CA/NY/TX) gates the production charge flip, not the code merge. **Status:** in-review.
- [2026-05-11_label-cancel-and-change_reviewed-2026-05-12_decided-2026-05-12.md](2026-05-11_label-cancel-and-change_reviewed-2026-05-12_decided-2026-05-12.md) — user-facing Cancel + Change buttons on `/t/<public_code>` while `status='label_created'`. Extends [`cancel-label`](../supabase/functions/cancel-label/index.ts) with email-token auth (sender's "Label ready" email carries `?cancel=<hex>`; sessionStorage holds it across reloads), Stripe refund via `shipments.stripe_payment_intent_id` (per Stripe Phase A's split-brain-prevention rule, cancel-label sets `refund_status='submitted'`; stripe-webhook advances to `refunded` on `charge.refunded` and writes the −refund ledger row), and link revival (`in_use → active` when carrier void succeeds and no other non-terminal shipment exists). Migration 020 renames `used → in_use` and adds `completed` to the link enum + `cancel_token` to shipments. Email becomes required at sender Step 4 with copy "It's important to have a reachable email in case you want to change your shipment." Iteratively reviewed with John in three Q&A rounds; decided 2026-05-12. **Status:** decided, implementation starting. Phase B items deferred (cancel email template, Dashboard cancel button, /s state messages, multi-billing audit).
- [2026-05-11_account-creation-timing_reviewed-2026-05-11_decided-2026-05-11.md](2026-05-11_account-creation-timing_reviewed-2026-05-11_decided-2026-05-11.md) — resolves Stripe proposal §11 #4 (the last Phase A blocker). Recommends Pattern A: auto-create the Supabase auth user via Supabase-native OTP before the payment step in the Full Prepaid Label flow. Replaces the bespoke `email_verifications` OTP with `signInWithOtp` so verifying the email IS the account creation; adds a "Continue with Google" affordance on the same step. Review (approve-with-changes) surfaced 5 blocking issues: shared `RecipientStepEmailVerify` rewrite silently mutates the flex flow (contradicts "no flex change"); Google-OAuth path produces an email that may disagree with the step-1-typed email; Supabase identity-linking dashboard flip is a hard ship blocker, not a soft pre-req; comp-mode placeholder `user_id` still trips migration 012's FK; LOC budget undersells the auth-JWT plumbing through `buyLabel`. Author response accepted 4 of 5 blockers outright (split shared component, lock to OAuth email + disclosure, system profile-row migration for comp UUID, ~80 LOC for JWT plumbing → revised total ~420 LOC), escalated identity-linking + OTP-step placement to John as two tradeoffs. **Decided 2026-05-11:** T2 = verify step between rates and payment, with `signInWithOtp` firing at step 1 the moment the email field commits so the code is in the inbox by verify time (John's call — protect the Guestimator demo + give the email time to arrive). T1 = parallel verification (John checks Supabase identity-linking during implementation; failure surfaces at Phase B, not this PR). **Status:** decided, ready for implementation. Stripe Phase A unblocked.
- [2026-05-11_sender-flow-wizard_reviewed-2026-05-11_decided-2026-05-11.md](2026-05-11_sender-flow-wizard_reviewed-2026-05-11_decided-2026-05-11.md) — build the 5-step sender wizard at `/s/:shortCode` end-to-end so flex links produce real EasyPost labels today. Routes around blocked Stripe Phase E via the existing `comp: true` path, hardened with admin-JWT-OR-active-flex-link gate, server-side `to_address`/recipient_email resolution, and server-derived cap enforcement. Review (approve-with-changes) surfaced 5 blocking issues — all accepted: `link_type` string mismatch, missing `public_code` in labels response, sender-can't-show-full-address, Stripe Phase A coordination, and client-supplied-price cap-bypass (PLAYBOOK Rule 14). Author folded all five + mobile-Safari iframe pivot to `<a target="_blank">`. **Round 2 (2026-05-11 evening):** promoted `/t/<public_code>` to be THE shipment page (label preview + Print/Download + lifecycle hero + Ship-Again upsell + terminal-state banners). After Confirm the sender's URL is replaced via `navigate(replace:true)` to `/t/<code>?fresh=1` — one URL per shipment, bookmarkable. Reviewed approve-with-changes (5 blockers, all accepted); John picked Option (a) on the privacy expansion. **Status:** decided 2026-05-11 (Round 1 + Round 2 both shipped).
- [2026-05-11_sendmo-public-tracking-code_reviewed-2026-05-11_decided-2026-05-11.md](2026-05-11_sendmo-public-tracking-code_reviewed-2026-05-11_decided-2026-05-11.md) — decouple `/track` URL from the carrier tracking number. Mint a SendMo-owned 7-char Crockford-base32 `public_code` per shipment; new canonical URL `sendmo.co/t/<code>`; legacy `/track/<carrier_number>` becomes a 301 redirect. Review surfaced that the actual bug today is "wrong shipment to wrong viewer" (`.single()` returns arbitrary matching row), not a 404 — collision severity is worse than originally framed. **Status:** decided 2026-05-11 (approved; implementation starting).
- [2026-04-26_stripe-integration-plan_reviewed-2026-04-26_decided-2026-05-11.md](2026-04-26_stripe-integration-plan_reviewed-2026-04-26_decided-2026-05-11.md) — wiring Stripe + ledger + reconciliation so SendMo can collect real money. **Status:** decided 2026-05-11. Six §11 decisions locked in (refund-to-card, $1 flat fee, D-then-C w/ mandate, 3-mode admin + role-based auth, carrier-adjustment caps). #4 (account-creation timing) deferred for research and blocks Phase A. #6/#9/#11 deferred to later phases. Phase 1 (full-label test-mode) already shipped in commit `90aebca`.
- [2026-04-26_links-manager_reviewed-2026-04-26_decided-2026-04-26.md](2026-04-26_links-manager_reviewed-2026-04-26_decided-2026-04-26.md) — auth-aware `/links/new` + `/links/:id/edit` outside the onboarding wizard, so returning users don't get OTP'd or asked to re-authorize a card. Refactors wizard step components into reusable presenters. **Status:** decided 2026-04-26 (approved; implementation in progress).
