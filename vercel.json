{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/s/:shortCode", "destination": "/api/s/:shortCode" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
